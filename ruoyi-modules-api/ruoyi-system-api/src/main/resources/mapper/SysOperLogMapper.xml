<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ruoyi.system.mapper.SysOperLogMapper">

    <!-- 查询用户操作热力图数据 -->
    <select id="selectUserOperationHeatmapData" resultType="java.util.HashMap">
        SELECT
            oper_url as operUrl,
            DATE_FORMAT(oper_time, '%Y-%m-%d') as date,
            HOUR(oper_time) as hour
        FROM sys_oper_log
        WHERE status = 0
          AND oper_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY oper_time DESC
    </select>

</mapper>
