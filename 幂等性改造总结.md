# 后端幂等性改造完整总结

## 改造背景

修复前端 FragmentSelector 组件发现的并发问题：用户点击批量关联时，由于先查后插的竞态条件，导致重复请求报错"该条目与片段已存在关联"。

## 核心问题

### 1. 先查后插模式的竞态条件
```java
// 问题代码
if (itemFragmentMapper.existsAssociation(itemUuid, fragmentId)) {
    throw new ServiceException("该条目与片段已存在关联");
}
// 并发场景下，两个线程同时通过检查，都执行插入，导致重复数据
itemFragmentMapper.insert(entity);
```

### 2. JavaScript Number 精度丢失
前端使用 `parseInt()` 转换 19 位数字 ID，导致精度丢失，后端接收到错误的 ID。

### 3. 删除操作非幂等
删除不存在的记录时抛出异常，不符合幂等语义。

## 改造文件清单

### 高优先级（核心关联操作）

| 序号 | 文件 | 改造方法 | 核心变更 |
|------|------|----------|----------|
| 1 | `KnowledgeItemFragmentServiceImpl.java` | `associate()` | 幂等插入，捕获 DuplicateKeyException |
|  |  | `batchAssociate()` | 批量幂等，单条失败不影响整体 |
|  |  | `disassociate()` | 删除幂等，不抛异常 |
|  |  | `disassociateAll()` | 批量删除幂等 |
| 2 | `KnowledgeAttachServiceImpl.java` | `insertByBo()` | 同名文件幂等上传 |
|  |  | `validEntityBeforeSave()` | 移除重名检查异常 |
| 3 | `KnowledgeFavoriteServiceImpl.java` | `insertByBo()` | 重复收藏幂等 |
|  |  | `deleteByItemUuid()` | 取消收藏幂等 |
| 4 | `KnowledgeTagServiceImpl.java` | `addTagToItem()` | 重复添加标签幂等 |
|  |  | `removeTagFromItem()` | 移除标签幂等 |

### 中优先级（删除操作）

| 序号 | 文件 | 改造方法 | 核心变更 |
|------|------|----------|----------|
| 5 | `SysUserServiceImpl.java` | `deleteUserById()` | 删除用户幂等 |
|  |  | `deleteUserByIds()` | 批量删除幂等 |

### 前端配合改造

| 文件 | 改造内容 |
|------|----------|
| `FragmentSelector.vue` | ID 精度修复：移除 `parseInt()`，直接使用字符串 ID |
|  | 防抖保护：防止重复点击 |
|  | 详细日志：便于调试 |
| `fragment.ts` | API 类型：支持 `string \| number` 类型 ID |

## 幂等性实现模式

### 模式一：查询+异常捕获（推荐）
```java
@Override
@Transactional(rollbackFor = Exception.class)
public KnowledgeItemFragment associate(String itemUuid, Long fragmentId) {
    // 1. 先查询，避免不必要的插入尝试
    KnowledgeItemFragment existing = itemFragmentMapper.selectOne(...);
    if (existing != null) {
        return existing; // 已存在，幂等返回
    }

    // 2. 尝试插入
    try {
        itemFragmentMapper.insert(entity);
        return entity;
    } catch (DuplicateKeyException e) {
        // 3. 并发场景下其他事务已插入，查询并返回
        return itemFragmentMapper.selectOne(...);
    }
}
```

### 模式二：删除操作幂等
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void disassociate(String itemUuid, Long fragmentId) {
    int deleted = itemFragmentMapper.delete(wrapper);
    // 幂等：删除0条也是成功（本来就没有关联）
    if (deleted > 0) {
        log.info("删除 {} 条记录", deleted);
    } else {
        log.debug("记录不存在，幂等处理");
    }
}
```

### 模式三：批量操作单条保护
```java
@Override
@Transactional(rollbackFor = Exception.class)
public List<KnowledgeItemFragment> batchAssociate(...) {
    for (Long fragmentId : fragmentIds) {
        try {
            // 单条插入，失败不影响其他
            itemFragmentMapper.insert(entity);
        } catch (DuplicateKeyException e) {
            // 跳过已存在，继续处理下一条
            log.debug("跳过已存在: {}", fragmentId);
        }
    }
}
```

## 数据库索引建议

详见 `幂等性改造数据库索引建议.sql`，需要添加以下唯一索引：

1. `knowledge_item_fragment`: `(item_uuid, fragment_id)`
2. `knowledge_attach`: `(kid, doc_name)`
3. `knowledge_favorite`: `(user_id, item_uuid)`
4. `knowledge_item_tag`: `(item_uuid, tag_id)`

## 最佳实践总结

### DO（推荐）
- ✅ 使用数据库唯一索引保证并发安全
- ✅ 捕获 `DuplicateKeyException` 实现幂等，而非先查后插
- ✅ 删除操作不应因记录不存在而抛异常
- ✅ 批量操作时单条失败不影响整体
- ✅ 超长大数字 ID 使用字符串传递

### DON'T（避免）
- ❌ 先查询存在性再插入（竞态条件）
- ❌ 使用 `if (count > 0) throw new Exception()` 模式
- ❌ 删除操作因记录不存在而抛异常
- ❌ 使用 `parseInt()` 转换超长数字 ID
- ❌ 批量操作时一条失败整体回滚（除非业务要求）

## 验证方法

1. **并发测试**: 使用 JMeter 或 Postman 并发发送相同请求，验证是否都能成功
2. **前端验证**: 快速点击按钮，验证无重复关联异常
3. **日志检查**: 查看日志是否输出"幂等处理"等关键字

## 后续建议

1. 在数据库层添加唯一索引（见 SQL 文件）
2. 对其他 Service 进行类似审计，特别是：
   - 订单/支付相关操作（必须幂等）
   - 库存扣减（需要特殊处理，非简单幂等）
   - 消息队列消费（建议幂等）
3. 添加幂等性测试用例到自动化测试套件

## 改造成效

- **并发安全**: 消除竞态条件，支持高并发场景
- **用户体验**: 重复操作不再报错，提升体验
- **数据一致性**: 避免重复数据产生
- **系统健壮性**: 网络超时、自动重试等场景下表现稳定
